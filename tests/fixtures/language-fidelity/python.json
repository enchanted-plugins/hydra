{
  "_comment": "Idiomatic, genuinely vulnerable Python - one proving case per rule that claims language 'python'. A rule tagged python with no entry here, or an entry it fails to match, fails test-corpus-language-fidelity.py. 'context_bound' rules are exempt from the positive requirement because the property they describe is multi-line or whole-file and cannot be expressed in a single-line regex; that limitation is disclosed through the coverage contract instead of being papered over with more regexes.",
  "language": "python",
  "positive": {
    "command-injection-eval": "result = eval(request.args.get(\"expr\"))",
    "command-injection-os-system": "os.system(\"ping -c1 \" + host)",
    "command-injection-subprocess-shell": "out = subprocess.check_output(\"curl -sI \" + url, shell=True)",
    "cors-reflect-origin": "resp.headers[\"Access-Control-Allow-Origin\"] = request.headers.get(\"Origin\")",
    "cors-wildcard": "resp.headers[\"Access-Control-Allow-Origin\"] = \"*\"",
    "debug-mode-production": "app.run(host=\"127.0.0.1\", debug=True)",
    "deserialization-pickle": "obj = pickle.loads(payload)",
    "deserialization-yaml-unsafe": "cfg = yaml.load(raw)",
    "django-raw-sql": "qs = User.objects.raw(\"SELECT * FROM users WHERE id = %s\" % uid)",
    "flask-send-file-user-path": "return send_file(request.args.get(\"path\"))",
    "hardcoded-connection-string": "DATABASE_URL = \"postgres://admin:s3cretpw@db.internal:5432/app\"",
    "hardcoded-ip-bind-all": "app.run(host=\"0.0.0.0\", port=8080)",
    "hardcoded-ip-private": "REDIS_HOST = \"10.0.0.5\"",
    "hardcoded-password-literal": "password = \"sup3rs3cret\"",
    "hardcoded-secret-key": "SECRET_KEY = \"django-insecure-abc123def456ghi\"",
    "header-injection": "resp.headers[\"X-Next\"] = request.args.get(\"target\")",
    "http-response-splitting": "resp.headers[\"Location\"] = request.args.get(\"next\")",
    "information-exposure-stacktrace": "return traceback.format_exc(), 500",
    "insecure-cookie-no-httponly": "resp.set_cookie(\"session\", token, httponly=False)",
    "insecure-cookie-no-samesite": "resp.set_cookie(\"session\", token, samesite=None)",
    "insecure-cookie-no-secure": "resp.set_cookie(\"session\", token, secure=False)",
    "insecure-crypto-no-iv": "cipher = AES.new(key, AES.MODE_CBC)",
    "insecure-deserialization-jsonpickle": "obj = jsonpickle.decode(payload)",
    "insecure-deserialization-shelve": "db = shelve.open(user_path)",
    "insecure-http-url": "resp = requests.get(\"http://api.partner.com/v1/data\")",
    "insecure-random-python": "token = random.randint(0, 999999)",
    "insecure-websocket-ws": "ws = websocket.create_connection(\"ws://api.example.com/feed\")",
    "jwt-hs256-public-key": "claims = jwt.decode(token, public_key, algorithms=[\"HS256\"])",
    "jwt-no-verify": "claims = jwt.decode(token, options={\"verify_signature\": False})",
    "jwt-none-algorithm": "claims = jwt.decode(token, key, algorithms=[\"none\"])",
    "jwt-weak-secret": "tok = jwt.encode(payload, \"secret\", algorithm=\"HS256\")",
    "log-injection-python": "logger.info(\"user login: \" + request.args.get(\"user\"))",
    "nosql-injection-pymongo": "doc = db.users.find({\"name\": request.args.get(\"name\")})",
    "open-redirect": "return redirect(request.args.get(\"next\"))",
    "open-redirect-flask": "return flask.redirect(request.args.get(\"url\"))",
    "path-traversal-join": "fh = open(os.path.join(BASE_DIR, request.args.get(\"f\")))",
    "path-traversal-open": "fh = open(request.args.get(\"file\"))",
    "python-compile-user-input": "code = compile(request.form[\"src\"], \"<user>\", \"exec\")",
    "python-exec-user-input": "exec(request.form[\"code\"])",
    "regex-dos-python": "m = re.match(r\"(a+)+$\", user_input)",
    "sql-injection-concat": "cursor.execute(\"SELECT * FROM users WHERE id = \" + uid)",
    "sql-injection-format": "cursor.execute(\"SELECT * FROM users WHERE id = {}\".format(uid))",
    "sql-injection-fstring": "cursor.execute(f\"SELECT * FROM users WHERE id = {uid}\")",
    "sql-injection-percent": "cursor.execute(\"SELECT * FROM users WHERE id = %s\" % uid)",
    "ssrf-cloud-metadata": "r = requests.get(\"http://169.254.169.254/latest/meta-data/\")",
    "ssrf-internal-network": "r = requests.get(\"http://localhost:8080/admin\")",
    "ssrf-request": "r = requests.get(request.args.get(\"url\"))",
    "ssti-jinja2-from-string": "html = Template(request.args.get(\"tpl\")).render()",
    "ssti-jinja2-render-string": "html = render_template_string(request.args.get(\"tpl\"))",
    "ssti-mako-template": "html = mako.template.Template(user_tpl).render()",
    "tls-verify-disabled": "r = requests.get(url, verify=False)",
    "weak-crypto-des": "cipher = DES.new(key, DES.MODE_ECB)",
    "weak-crypto-ecb-mode": "cipher = AES.new(key, AES.MODE_ECB)",
    "weak-crypto-rsa-small-key": "key = RSA.generate(1024)",
    "weak-hash-md5": "digest = hashlib.md5(data).hexdigest()",
    "weak-hash-sha1": "digest = hashlib.sha1(data).hexdigest()",
    "weak-password-hash-md5": "stored = hashlib.md5(password.encode()).hexdigest()",
    "xml-bomb-billion-laughs": "<!ENTITY lol \"lol\">",
    "xpath-injection": "nodes = tree.xpath(\"//user[name='\" + name + \"']\")",
    "xxe-lxml-python": "doc = etree.parse(fh, etree.XMLParser(resolve_entities=True))",
    "xxe-parse": "tree = xml.etree.ElementTree.parse(user_file)"
  },
  "taint_bound": {
    "cors-credentials-wildcard": "wildcard origin and credentials=True are separate arguments/lines; single-line matching cannot correlate them",
    "graphql-introspection-enabled": "framework-specific server config, commonly split across lines",
    "insecure-file-upload-python": "requires knowing the object is an upload handle; the call itself is ordinary",
    "ldap-injection-search": "requires knowing the receiver is an LDAP connection; conn.search() is generic",
    "mass-assignment-django": "class-level Meta declaration spanning multiple lines",
    "missing-rate-limit-api": "detects the ABSENCE of a decorator, which needs whole-file reasoning",
    "race-condition-toctou-python": "two statements (exists then open); inherently multi-line",
    "session-fixation": "detects absence of session regeneration across a login flow",
    "session-no-regenerate": "detects absence of session regeneration across a login flow",
    "timing-attack-python": "comparing secrets with ==; matching every == would be unusable noise"
  },
  "negative": [
    "digest = hashlib.sha256(data).hexdigest()",
    "token = secrets.token_urlsafe(32)",
    "cursor.execute(\"SELECT * FROM users WHERE id = %s\", (uid,))",
    "r = requests.get(\"https://api.partner.com/v1/data\", timeout=10)",
    "out = subprocess.check_output([\"curl\", \"-sI\", url])",
    "cfg = yaml.safe_load(raw)",
    "app.run(host=\"127.0.0.1\", debug=False)",
    "resp.set_cookie(\"session\", token, secure=True, httponly=True, samesite=\"Lax\")",
    "cipher = AES.new(key, AES.MODE_GCM)",
    "key = RSA.generate(4096)",
    "claims = jwt.decode(token, key, algorithms=[\"RS256\"])",
    "stored = bcrypt.hashpw(password.encode(), bcrypt.gensalt())",
    "if hmac.compare_digest(token, expected_token):",
    "r = requests.get(url, verify=True)"
  ]
}
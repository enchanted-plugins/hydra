# Codex layer notes

GENERATED by codex-sync.py. The Claude Code plugins are mirrored as native
Codex plugins (`.agents/plugins/marketplace.json` + `plugins/*/.codex-plugin/`).
Skills are unchanged — `plugins/*/skills/*/SKILL.md` is the same path in Codex.

## Install

```
codex plugin marketplace add <repo>/.agents/plugins/marketplace.json
codex plugin list
```

## Hooks

`hooks` is rejected from plugin.json by Codex's validator. These Claude
hooks remain as companion folders (auto-discovered; same 10-event schema):
- `plugins/action-guard/hooks/hooks.json`
- `plugins/audit-trail/hooks/hooks.json`
- `plugins/canary/hooks/hooks.json`
- `plugins/capability-fence/hooks/hooks.json`
- `plugins/capability-shield/hooks/hooks.json`
- `plugins/config-shield/hooks/hooks.json`
- `plugins/egress-monitor/hooks/hooks.json`
- `plugins/egress-shield/hooks/hooks.json`
- `plugins/package-gate/hooks/hooks.json`
- `plugins/secret-scanner/hooks/hooks.json`
- `plugins/state-integrity/hooks/hooks.json`
- `plugins/vuln-detector/hooks/hooks.json`
Note: commands using `${CLAUDE_PLUGIN_ROOT}` won't expand under Codex — rewrite to MCP/absolute.

## Scripts → MCP (CLAUDE_PLUGIN_ROOT has no Codex equivalent)

Expose these via a per-plugin `.mcp.json` companion + `mcpServers` manifest key:
- `shared/scripts/config-scanner.py`
- `shared/scripts/entropy-analyzer.py`
- `shared/scripts/learnings.py`
- `shared/scripts/pattern-engine.py`
- `shared/scripts/report-gen.py`
- `shared/scripts/supply-chain.py`
- `shared/scripts/vuln-scanner.py`
